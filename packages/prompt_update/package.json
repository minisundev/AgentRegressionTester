{
  "name": "prompt-update",
  "version": "1.0.0",
  "description": "Local prompt files → Redis sync server (replaces db_to_redis prompt sync)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/server.js",
    "dev": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^5.2.5",
    "dotenv": "^16.0.0",
    "fastify": "^5.6.1",
    "ioredis": "^5.8.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
