PORT=8083

# 프롬프트 md 파일 + manifest.yaml 이 있는 디렉토리 (레포 루트 기준 기본값: ../../prompts)
# PROMPTS_DIR=../../prompts

# 파일 저장 시 자동 sync (true 로 켜면 api 호출 없이도 저장 즉시 반영)
WATCH=false

# Redis (메인 레포 .env.local에서 복사)
REDIS_ENDPOINT=localhost
REDIS_PORT=6379
REDIS_PASSWD=
REDIS_SSL=false
